项目包结构
    com.neuedu
        .controller 控制层
        .service 业务逻辑
        .dao 持久层
        .common 工具包
        .pojo 实体类包
        .const 常量包
1、后台登录接口
    dao层：jdbc
        接口+实现类
        jdbc:0、引入依赖 1、加载驱动 2、获取连接 3、preparedStatement 4、执行sql 5、处理结果 6、关闭连接
2、缓存
    在内存中开辟空间，将数据保存到缓存中
    当用户第一次访问商品数据时，程序读缓存数据，如果缓存中没有数据，会读mysql，从mysql获取到数据后会将
    数据放到缓存中，然后将数据返回到客户端
    当用户第二次访问商品数据，程序读缓存数据，如果有返回到客户端。
